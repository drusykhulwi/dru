# Contributing
How to contribute: branching model, commit messages, PR rules

- Branch from `dev` using `feature/*` naming.
- Use clear commit messages (e.g., "Add login feature").
- Open PRs to `dev` and wait for review before merging.